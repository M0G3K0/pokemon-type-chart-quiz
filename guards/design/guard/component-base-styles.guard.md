<!-- 🛡️ GUARDRAIL -->

# コンポーネント基本スタイル (component-base-styles)

## @what / @why / @failure

```
@what  UIコンポーネントの :host スタイルを一元管理し、重複を防止する
@why   全コンポーネントで一貫したパターンを使用し、変更が1箇所で済むようにするため。AIが新規コンポーネント作成時に正しいパターンを強制する。
@failure  共通スタイルがバラバラに定義され、変更時に全ファイル修正が必要になる。CIでエラーとなる。
```

---

## ルール一覧

### RULE-01: `:host` 内で直接 `display` を定義してはならない

コンポーネントSCSSファイル（`src/app/ui/**/*.scss`）において、
`:host` セレクタ内で `display` プロパティを直接定義することを禁止する。

**❌ 禁止パターン:**

```scss
:host {
  display: inline-block;
}
```

**✅ 正しいパターン:**

```scss
@use '../../../styles/component-base' as base;

:host {
  @include base.host-inline;
}
```

### RULE-02: `:host` 内で直接 `line-height` を定義してはならない

画像コンポーネントで `line-height: 0` を直接定義することを禁止する。

**❌ 禁止パターン:**

```scss
:host {
  display: inline-block;
  line-height: 0;
}
```

**✅ 正しいパターン:**

```scss
@use '../../../styles/component-base' as base;

:host {
  @include base.host-image;
}
```

---

## ホストタイプ一覧

`src/styles/_component-base.scss` で定義されているMixin：

| Mixin | 用途 | 使用コンポーネント例 |
|-------|------|---------------------|
| `host-inline` | インライン要素 | button, chip |
| `host-block` | ブロック要素 | card, heading, surface |
| `host-image` | 画像表示（line-height: 0） | avatar, icon |
| `host-inline-text` | テキストラッパー | text |

---

## 新規コンポーネント作成時の手順

1. コンポーネントの表示タイプを決定する（上記テーブル参照）
2. SCSSファイルの冒頭に `@use '../../../styles/component-base' as base;` を追加
3. `:host` セレクタで適切なMixinを使用
4. コンポーネント固有のスタイルは `.pt-xxx` クラスで定義

**例:**

```scss
/**
 * @what 新規コンポーネントのスタイル定義
 * @why デザイントークンとMixinを使用して一貫したスタイルを保証
 */

@use '../../../styles/component-base' as base;

:host {
  @include base.host-inline;
}

.pt-new-component {
  // コンポーネント固有のスタイル
}
```

---

## 実装

- **共通Mixin**: `src/styles/_component-base.scss`
- **検証ルール**: `guards/design/rules/component-base-styles.rules.js`

---

## 違反時の対応

1. エラーメッセージで指摘された `:host` 内の直接スタイル定義を削除
2. `@use '../../../styles/component-base' as base;` を追加（未追加の場合）
3. 適切な `@include base.host-*` を使用
4. ビルドを再実行して検証

---

## 参照

- [Issue #57](https://github.com/M0G3K0/pokemon-type-chart-quiz/issues/57): 共通スタイルの一元管理
