<!-- 🛡️ GUARDRAIL -->

# CSSトークン存在確認

## @what / @why / @failure

```
@what  SCSSファイル内で使用されているCSS変数（--pt-*）が実際に定義されているか検査
@why   トークン名のtypoがビルド時に検出されず、本番で初めて見た目が崩れる問題を防ぐため
@failure  未定義のトークンを使用しているファイルはCIでエラーとなる
```

---

## 背景

### 実際に起きた問題

`pt-chip.scss` で `--pt-border-radius-sm` と記述したが、実際のトークン名は `--pt-semantic-border-radius-sm` だった。このようなトークン名の誤りがビルド時にエラーにならず、**本番環境で初めて見た目が崩れる**問題が発生した。

### このガードレールが解決すること

- ✅ トークン名のtypoを即座に検出
- ✅ 存在しないトークンへの参照をCIで阻止
- ✅ 正しいトークン名へのガイダンス提供

---

## ルール一覧

### 1. 検査対象

```
src/**/*.scss
```

### 2. 抽出パターン

```css
var(--pt-*)
```

### 3. 定義ファイル

```
src/styles/generated/tokens.css
```

このファイルに定義されている `--pt-*` 変数と照合。

---

## 実装

### ルールファイル

**ソースオブトゥルース**: [`token-existence.rules.js`](../rules/token-existence.rules.js)

### 使用方法

```bash
npm run guard:token-existence
```

---

## 違反時の対応

1. エラーメッセージで未定義トークンとファイルを確認
2. `src/styles/generated/tokens.css` で正しいトークン名を検索
3. SCSSファイルを修正
4. `npm run guard:token-existence` で再検証

### 例

```
❌ Undefined token: --pt-border-radius-sm
   Used in: src/app/ui/pt-chip/pt-chip.scss:15
   Did you mean: --pt-semantic-border-radius-sm ?
   See: guards/design/guard/token-existence.guard.md
```

---

## 関連

- [Guards README](../../README.md)
- [Token Naming Guard](./token-naming.guard.md)
- [Design Tokens Reference](../../../docs/design-tokens.md)
