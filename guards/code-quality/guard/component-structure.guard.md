<!-- 🛡️ GUARDRAIL -->

# コンポーネント構成ファイル完全性チェック

## @what / @why / @failure

```
@what  pt-*コンポーネントが必須ファイル（ts/spec.ts）を含んでいることを検査
@why   ファイル不足によるテスト欠落や保守性低下を防ぐため
@failure  必須ファイルが不足しているコンポーネントはCIでエラーとなる
```

---

## ルール一覧

### 1. 必須ファイル

すべての `pt-*` コンポーネントディレクトリには以下が必須：

| ファイル | 必須 | 説明 |
|----------|:----:|------|
| `pt-{name}.ts` | ✅ | Componentクラス |
| `pt-{name}.spec.ts` | ✅ | テストファイル |
| `pt-{name}.scss` | ⚪ | スタイル（インライン可） |
| `pt-{name}.html` | ⚪ | テンプレート（インライン可） |

### 2. 対象ディレクトリ

```
src/app/ui/pt-*/
```

### 3. 許容パターン

- **インラインスタイル**: `.scss` ファイルがなくてもOK（`styles`プロパティで定義）
- **インラインテンプレート**: `.html` ファイルがなくてもOK（`template`プロパティで定義）

---

## 実装

### ルールファイル

**ソースオブトゥルース**: [`component-structure.rules.js`](../rules/component-structure.rules.js)

### 使用方法

```bash
npm run guard:component-structure
```

---

## 違反時の対応

1. エラーメッセージで不足ファイルを確認
2. 不足ファイルを作成（テンプレートは `ng generate component` 参照）
3. `npm run guard:component-structure` で再検証

### 例

```
❌ Missing required file: src/app/ui/pt-button/pt-button.spec.ts
   See: guards/code-quality/guard/component-structure.guard.md
```

---

## 関連

- [Guards README](../../README.md)
- [Test Existence Guard](../../maintenance/guard/test-existence.guard.md)
