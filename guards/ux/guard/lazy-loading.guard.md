<!-- 🛡️ GUARDRAIL -->

# 遅延ロードの強制（UXの憲法）

## @what / @why / @failure

```
@what  ルート定義におけるLazy Loadingの使用を検査
@why   初期バンドルサイズを削減し、アプリケーションの起動速度（TTVD）を向上させるため
@failure  Eager Loading（即時読み込み）が検出された場合、CIが失敗する
```

---

## ルール一覧

### 1. ルートコンポーネントの遅延読み込み

`app.routes.ts` において、`component: MyComponent` の形式（Eager Loading）を禁止し、`loadComponent: () => import(...)` （Lazy Loading）の使用を強制する。

**理由**: Eager Loadingは、そのコンポーネントが依存する全てのコードを初期バンドルに含めてしまうため。

---

## 例外と理由

### 1. レイアウトコンポーネント

- **対象**: `LayoutComponent` 等
- **理由**: アプリケーションの骨格として必ず初期表示されるものはEager Loadingでも構わない（が、Lazyでも良い）。

---

## 実装

### ルールファイル（カスタムスクリプト）

**ソースオブトゥルース**: [`lazy-loading.rules.js`](../rules/lazy-loading.rules.js)

### 使用方法

```bash
npm run lint:ux
```

---

## 違反時の対応

1. `app.routes.ts` を開く
2. `component: MyComponent` を `loadComponent: () => import('./path/to/my.component').then(m => m.MyComponent)` に書き換える

---

## 関連

- [Guards README](../../README.md)
