# pt-spinner 使用ガイドライン

## @what / @why
- @what: スピナー（ローディングインジケーター）の使用基準
- @why: 不必要なローディング表示はUXを損なう。適切な状況でのみ使用することでユーザー体験を向上させる。

---

## ✅ 使用すべきケース

### 1. 長時間の非同期処理（300ms以上）
- 大量データの取得（100件以上のリスト）
- 画像や動画などの重いアセットのロード
- サーバーサイドの計算処理

### 2. ユーザーアクション後の待機
- フォーム送信後の処理中
- ボタンクリック後の API 呼び出し
- ファイルアップロード中

### 3. 初回ロードが遅い可能性がある画面
- 外部 API に依存する画面
- 大量のデータを表示するダッシュボード
- 初期化に時間がかかるコンポーネント

---

## ❌ 使用すべきでないケース

### 1. 瞬時に完了する処理（300ms未満）
- ローカルデータの表示
- キャッシュ済みのデータ取得
- 軽量な状態変更

**問題**: スピナーが一瞬だけ表示され、かえってチラつきを生む

### 2. コンテンツが既に画面にある場合
- インクリメンタルなデータ追加
- 部分的な更新

**代替案**: Skeleton UI やインライン更新を使用

### 3. バックグラウンド処理
- ユーザーが待つ必要がない非同期処理
- 自動保存・自動同期

---

## 🎯 推奨パターン

### Delayed Loading Pattern
300ms 未満で完了する処理ではスピナーを表示しない。

```typescript
// ❌ 悪い例: 常にスピナーを表示
this.isLoading = true;
await fetchData();
this.isLoading = false;

// ✅ 良い例: 遅延してからスピナーを表示
const timer = setTimeout(() => this.isLoading = true, 300);
await fetchData();
clearTimeout(timer);
this.isLoading = false;
```

### Skeleton UI との使い分け
| シチュエーション | 推奨UI |
|------------------|--------|
| コンテンツの形がわかっている | Skeleton |
| 処理時間が不明・長い | Spinner |
| 部分的な更新 | インラインSpinner |

---

## 📏 サイズの使い分け

| サイズ | 用途 |
|--------|------|
| `sm` | ボタン内、インラインローディング |
| `md` | カードやセクション内 |
| `lg` | 全画面ローディング、メインコンテンツエリア |

---

## 🔗 参考資料

- [Material Design 3: Progress Indicators](https://m3.material.io/components/progress-indicators/overview)
- [Nielsen Norman Group: Response Time Limits](https://www.nngroup.com/articles/response-times-3-important-limits/)
  - 0.1秒: 即座に感じる
  - 1秒: 思考が中断されない
  - 10秒: 注意を維持する限界
